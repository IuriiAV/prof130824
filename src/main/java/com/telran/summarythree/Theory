ArrayList - динамический массив (список)
LinkedList - связный список
Set - неупорядоченній набор уникальніх єлементов


ArrayList VS LinkedList

List VS Set

LinkedList :
Node  - Элемент связного списка, состоит из данных которые она хранит и ссылок на предыдущий и следующий элемент

ArrayList :
array(массив)

Set :
TreeSet : (Tree - > TreeNode - > value, leftNode, rightNode)
єлементі отсортировані

HashSet : HashTable - in Java данная структура основана на массиве  связных списков
массив связных списков

При попытке поместить элемент в HashSet или найти элемент,
сначала у элемента высчитывается hashcode О(1),
на основании этого кода и длины массива исчется подходящая ячейка храненияО(1) и если в этой ячейке пусто -
то создается связный список и наш элемент становится головным в этом связном списке О(1)

В идеальной ситуации в каждой ячейке не более одного элемента

capacity 0.75 от размерности нашего массива
После заполнения внктреннего массива до указанной емкости , массив увеличивается в 2 раза,
и так как в расчете позиции элемента участвуют длина массива,
то после увеличения все элементы по новой перераспределяются по ячейкам
